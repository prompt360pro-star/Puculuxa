version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: puculuxa_postgres
    restart: always
    environment:
      POSTGRES_USER: puculuxa
      POSTGRES_PASSWORD: puculuxa_secret
      POSTGRES_DB: puculuxa_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U puculuxa"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
